name: PSS Edge Fast V1.3 — DESATIVADO

on:
  workflow_dispatch:

jobs:
  noop:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "Workflow legado V1.3 desativado. Versão V1.4 permanece ativa."
