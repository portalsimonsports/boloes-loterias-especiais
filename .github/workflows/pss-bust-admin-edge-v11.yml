name: PSS Bust Admin Edge V1.1 — DESATIVADO

on:
  workflow_dispatch:

jobs:
  noop:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "Workflow legado V1.1 desativado. Versão V1.4 permanece como referência atual."
