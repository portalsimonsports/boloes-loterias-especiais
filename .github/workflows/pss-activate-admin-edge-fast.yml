name: PSS Activate Admin Edge Fast V1 — DESATIVADO

on:
  workflow_dispatch:

jobs:
  noop:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "Workflow legado desativado. Versões V1.4 permanecem ativas."
