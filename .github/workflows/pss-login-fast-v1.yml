name: PSS Login Fast V1 — DESATIVADO

on:
  workflow_dispatch:

jobs:
  noop:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "Workflow obsoleto. Substituído por pss-login-fast-v2-cachebust.yml"
